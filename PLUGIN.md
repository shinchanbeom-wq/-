# EndermanBoss Paper Plugin

이 저장소는 `.exe`, `.dll`, `.jar`, `.bin`, 이미지 파일, 압축 파일, AI 모델 파일, Git LFS 파일 없이 **소스코드와 JAR 생성용 BAT 파일만** 제공합니다.

## BAT 파일로 바로 JAR 만들기

Windows에서 저장소 루트 폴더의 아래 파일을 더블클릭하면 JAR가 생성됩니다. PC에 Gradle이 없어도 BAT 파일이 로컬 Gradle을 자동으로 다운로드해서 빌드합니다.

```text
build_plugin.bat
```

CMD에서 직접 실행하려면 아래 명령어를 사용하세요.

```cmd
build_plugin.bat
```

이미 Gradle이 설치되어 있다면 배치 파일 없이 직접 아래 명령어를 사용해도 됩니다.

```cmd
gradle clean build
```

빌드가 성공하면 아래 경로에 Paper 플러그인 JAR가 생성됩니다. BAT 파일이 받은 로컬 Gradle과 ZIP 파일은 `.gradle-local/` 아래에만 저장되고 Git에는 포함되지 않습니다.

```text
build\libs\enderman-boss-paper-1.0.0.jar
```

생성된 JAR 파일을 Paper 서버의 `plugins` 폴더에 복사한 뒤 서버를 재시작하세요.

## 서버에서 사용하기

OP 또는 `endermanboss.spawn` 권한이 있는 플레이어가 아래 명령어로 보스를 소환합니다. 보스 설명은 `/bossinfo enderman`으로 확인합니다.

```text
/bossspawn enderman
```

## 보스 정보

- 체력: 500
- 1페이즈: 500~250 HP, 뒤 텔레포트 공격 또는 도주 후 HP 10 추적 공허탄 다중 발사
- 2페이즈: 250~100 HP, 회복 엔드마이트 15마리, 공허웅덩이, 직선 돌진, 8연속 텔레포트 공격, 하늘 공허탄, 자폭 엔드마이트, 엔드 기둥
- 3페이즈: 100~0 HP, 30초 정지 및 받는 피해 0.8배 적용, 8초마다 파동 공격
- 3페이즈 제한 시간 내 처치 실패: 큰 보라색 폭발 후 보상 없음
- 엔더맨은 기본 텔레포트를 하지 않고 텔레포트 패턴에서만 순간이동
- 보스바가 표시되며 페이즈마다 색상과 전환 이펙트가 변경됨
- 철퇴 피해는 0.6배로 감소하며 다른 피해 감소와 중첩 적용
- 처치 성공 보상: 우클릭 시 가장 가까운 발사체 제외 대상 뒤로 이동하는 `공허의 단검`
- 공허의 단검 텔레포트 쿨타임: 5초
